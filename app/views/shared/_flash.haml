- unless flash.blank?
  .container.flash
    .row
      .flash_notices
        - flash.each do |key, msg|
          %div{:id => key, :class => "flash #{key}"}
            %span.icon
            %p.message
              = msg unless msg.is_a?(Array)